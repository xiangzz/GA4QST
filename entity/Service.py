class Service:

    def __init__(self, serviceId, requestSize, executeRate, unitPrice):
        self.serviceId = serviceId
        self.requestSize = requestSize
        self.executeRate = executeRate
        self.unitPrice = unitPrice